# Copy to .env and fill in. .env is gitignored, never commit real credentials.
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=change-me
# The app creates this database automatically on first start.
PGDATABASE=marketplace
